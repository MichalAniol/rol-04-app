namespace starter {
    export const run = async () => {
        await user.init()

            await data.check()


        // config i porównanie

        // pobieranie danych:
        // - pytania
        // - zdjęcia
    }
}