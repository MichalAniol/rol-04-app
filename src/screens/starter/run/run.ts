namespace starter {
    export const run = async () => {
        await user.init(data.check)
        // await data.check()


        // config i porównanie

        // pobieranie danych:
        // - pytania
        // - zdjęcia
    }
}