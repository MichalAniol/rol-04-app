namespace starter {
    const { byId, add } = dom



    // czy połączenie z netem
    export const init = async () => {


    }

    export const run = async () => {




        // const secure = await queries.secure.getSecure()
        // console.log('%c secure:', 'background:rgb(0, 42, 255); color: #003300', secure)

        // if (secure.state !== queries.responseState.ok) {

        // } else {

        // }

        // console.log('%c secure:', 'background: #ffcc00; color: #003300', secure)
        // // setTimeout(queries.secureTest, 3000)

        // if (secure === null) {

        // }
    }

    // czy nowy użytkownik
    // jak nie to wprowadzanie
    // lub qr code

    // config i porównanie

    // pobieranie danych:
    // - pytania
    // - zdjęcia



    export const active = () => {}

    export const deactivate = () => {}
}